# Pinned exactly: this stack had breaking changes across recent majors,
# so the verified combination is what the code is written against.
langgraph==1.2.9
langchain==1.3.14
langchain-google-genai==4.3.1

# Lower bounds only: any recent release works, and pinning would break
# installs on other Python versions (e.g. numpy 2.5 requires Python 3.12+).
numpy>=1.26
python-dotenv>=1.0
pytest>=8.0
